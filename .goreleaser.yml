version: 2

project_name: ajolote

before:
  hooks:
    - go mod tidy

builds:
  - id: ajolote
    main: ./cmd/ajolote
    binary: ajolote
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - id: ajolote
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - LICENSE*
      - README*

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

changelog:
  sort: asc
  use: git
  filters:
    exclude:
      - "^Merge"
      - "^chore:"
  groups:
    - title: "Features"
      regexp: "^[Aa]dd|^[Ii]mplement|^[Ss]upport|^[Gg]enerate"
      order: 0
    - title: "Bug Fixes"
      regexp: "^[Ff]ix|^[Cc]orrect"
      order: 1
    - title: "Other Changes"
      order: 999

release:
  github:
    owner: antoumpas
    name: ajolote-ai
  draft: false
  prerelease: auto
  name_template: "v{{.Version}}"

# Uncomment and configure once you have a homebrew-tap repo and HOMEBREW_TAP_TOKEN secret.
# homebrew_casks:
#   - name: ajolote
#     repository:
#       owner: ajolote-ai
#       name: homebrew-tap
#     commit_author:
#       name: ajolote-bot
#       email: bot@ajolote.ai
#     homepage: https://github.com/ajolote-ai/ajolote
#     description: "Shared AI agent configuration for multi-tool teams"

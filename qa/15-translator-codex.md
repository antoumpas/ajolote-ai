# 15 — Codex CLI Translator

Tests for `ajolote use codex` (generate) and Codex import.

**Note:** Codex uses TOML format for MCP config. It reads `AGENTS.md` natively for rules (does not generate its own rules file). It always creates `.codex/config.toml` even with no MCP servers.

---

## Generate

### CODEX-001 — .codex/config.toml always created

**Prerequisites:** Initialized project with empty `"servers": {}` (no MCP servers).  
**Steps:**
1. `ajolote use codex`
2. `cat .codex/config.toml`

**Expected result:** `.codex/config.toml` exists. Contains at minimum a comment header. No error. File is detectable (allows `ajolote diff` and `ajolote sync` to recognize codex as active).  
**Pass / Fail:** ☐

---

### CODEX-002 — TOML format with [mcp.servers.<name>] sections

**Prerequisites:** Config has `"github": {"command": "npx", "args": ["-y", "@modelcontextprotocol/server-github"]}`.  
**Steps:**
1. `ajolote use codex`
2. `cat .codex/config.toml`

**Expected result:** File contains:
```toml
[mcp.servers.github]
command = "npx"
args = ["-y", "@modelcontextprotocol/server-github"]
```
Valid TOML structure.  
**Pass / Fail:** ☐

---

### CODEX-003 — Server keys in sorted order

**Prerequisites:** Config has servers `"zebra"`, `"alpha"`, `"middle"`.  
**Steps:**
1. `ajolote use codex`
2. `grep "\[mcp.servers" .codex/config.toml`

**Expected result:** Servers appear in alphabetical order: `alpha`, `middle`, `zebra`. Deterministic output regardless of JSON map iteration order.  
**Pass / Fail:** ☐

---

### CODEX-004 — Env section only when env map non-empty

**Prerequisites:** Config has one server with `"env": {"API_KEY": "secret"}` and one without `env`.  
**Steps:**
1. `ajolote use codex`
2. `cat .codex/config.toml`

**Expected result:** `[mcp.servers.<name>.env]` section present only for the server with env. Server without env has no env section.  
**Pass / Fail:** ☐

---

### CODEX-005 — Env var substitution applied to URL and env values

**Prerequisites:** Config has `"url": "${MY_MCP_URL}"` and `"env": {"KEY": "${MY_API_KEY}"}`. Both env vars are set: `export MY_MCP_URL="https://example.com"; export MY_API_KEY="token123"`.  
**Steps:**
1. `ajolote use codex`
2. `cat .codex/config.toml`

**Expected result:** `url = "https://example.com"` and `KEY = "token123"` in the TOML. Variables expanded.  
**Pass / Fail:** ☐

---

## Import

### CODEX-006 — Import AGENTS.md as rule file (if not ajolote-generated)

**Prerequisites:** `AGENTS.md` exists with hand-written content (no ajolote HTML comment header).  
**Steps:**
1. `ajolote init` (fresh project)
2. `cat .agents/rules/general.md`

**Expected result:** Content from `AGENTS.md` imported into `general.md`.  
**Pass / Fail:** ☐

---

### CODEX-007 — Skip ajolote-generated AGENTS.md

**Prerequisites:** `AGENTS.md` starts with the ajolote HTML comment header (`<!-- AGENTS.md — Generated by ajolote-ai...`).  
**Steps:**
1. `ajolote init` (fresh project)
2. `cat .agents/rules/general.md`

**Expected result:** `general.md` contains default boilerplate, not the generated `AGENTS.md` content.  
**Pass / Fail:** ☐
